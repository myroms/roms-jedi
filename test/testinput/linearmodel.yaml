# ROMS is configured with the JEDI driver and includes the NLM, TLM, and ADM kernels
# for the US West Coast WC13 test application.

geometry:
  project_dir: jediroms_wc13/JEDI/
  roms_stdinp: roms_wc13_20040103.in
  fields metadata: Data/fields_metadata.yaml
  ng: 1                                        # ROMS nested grid number

analysis variables: &vars [ssh, uocn, vocn, tocn, socn]

model:
  name: ROMS
  model variables: *vars
  tstep: PT1800S                               # WC13 has a 30 minutes NLM timestep 

initial condition:
  state variables: *vars
  date: 2004-01-03T00:00:00Z

background error:
  covariance model: romsCovar

linear model test:
  forecast length: PT96H                       # 96 hours (4 days) window ending on 2004-01-07T00:00:00Z

linear model:
  name: identity
  tstep: PT1800S                               # WC13 has a 30 minutes TLM/ADM timestep
